{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build",
    "render-postbuild": "cd client && npm install && npm install && npm run build"
  },
  "author": "saurabh",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "cheerio": "^1.0.0-rc.12",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "google-trends-api": "^4.9.2",
    "keyword-extractor": "^0.0.23",
    "mongoose": "^6.5.0",
    "path": "^0.12.7"
  },
  "engines": {
    "node": "16.15.1"
  }
}
